// Server-side only — client components get this via AppNameProvider
export const APP_NAME = process.env.APP_NAME?.trim() || 'StayTrack'
export const APP_SUBNAME = process.env.APP_SUBNAME?.trim() || ''
